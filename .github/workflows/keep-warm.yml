name: keep-warm

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: ping backend health check
        run: curl -sf "${{ vars.RENDER_URL }}/api/health"
